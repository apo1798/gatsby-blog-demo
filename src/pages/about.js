import React from "react"
import Layout from "../components/Layout"

// import { graphql } from "gatsby"

const AboutPage = ({ data }) => {
  return <Layout pageTitle="關於我"></Layout>
}

// export const query = graphql``

export default AboutPage
